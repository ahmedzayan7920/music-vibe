import 'package:flutter/material.dart';

class HomeFavorites extends StatelessWidget {
  const HomeFavorites({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Home Favorites"),
    );
  }
}
